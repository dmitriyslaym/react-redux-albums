import React from 'react';
import TestTestConnected from './TestTest.js';

const Test = (props) => (
	<div>
		<h1>{props.albums[0].title}</h1>
		<TestTestConnected />
	</div>
	);

export default Test;

